import { APIGatewayProxyEventV2, Handler } from "aws-lambda";

export const handler: Handler = async function (event: APIGatewayProxyEventV2) {
  // TODO: Delete channel from user saved channels
  return;
};
